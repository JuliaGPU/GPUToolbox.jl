var documenterSearchIndex = {"docs":
[{"category":"section","location":"overlays/#Overlays","page":"Overlays","text":"","title":"Overlays"},{"category":"module","location":"overlays/#GPUToolbox.Overlays","page":"Overlays","text":"GPUToolbox.Overlays\n\nMethod tables with device overrides of Base functionality, shared by GPU back-ends.\n\nSome of Base's methods are unsuited for GPUs, e.g., because they compute single-precision results in double precision, which many GPUs don't support. Back-ends replace such methods with overlay methods in their own method table. The tables in this module collect overrides that aren't specific to a back-end, so that back-ends can share them by stacking them underneath their own method table.\n\nUsage\n\nA back-end uses a table by stacking it underneath its own method table when it creates the method table view for a compiler job, e.g.:\n\nfunction GPUCompiler.method_table_view(job::MyCompilerJob)\n    if job.config.target.supports_fp64\n        GPUCompiler.StackedMethodTable(job.world, method_table)\n    else\n        GPUCompiler.StackedMethodTable(job.world, method_table,\n                                       GPUToolbox.Overlays.float64_overrides)\n    end\nend\n\nAs shown here, the tables to use can depend on the target device. The choice may only depend on the job's target and parameters, though, as GPUCompiler shares inference results between jobs with equal targets and parameters.\n\nLookup order\n\nThe tables in a stack are searched from the top: the first table with a method that covers the signature of a call provides the implementation, even if a table further down the stack, or Base, has a more specific method. The stack should be ordered from the most to the least device-specific knowledge:\n\nthe back-end's own method table, with implementations that use the device's hardware or vendor libraries;\ntables shared by a family of back-ends, like the one from SPIRVIntrinsics.jl, used by the back-ends targeting OpenCL environments;\nthe tables in this module;\nBase.\n\nBecause of that, the overrides here only act as fallbacks: a back-end replaces one by defining its own method with the same signature. Calls made by an override are again looked up from the top of the stack, so shared overrides use the back-end's implementations of the functions they call.\n\nRules\n\nTo keep the order of the stack from affecting correctness, overrides in this module:\n\nimplement the Base method they replace completely and correctly for their signature, so that the back-end's methods only have to be better, not different;\nconsist of generic Julia code, without calls to device-specific functionality;\nuse concrete signatures, so as not to hide more specific methods further down the stack;\nreplace a method in Base, or extend a Base function for a type defined in this module (checked by audit);\ndon't overlap with the overrides in other tables in this module, so that the relative order of these tables doesn't matter;\ndocument which functions they expect the stack to provide (e.g., Float64-free elementary functions).\n\nBack-ends can check their stack with audit, e.g., as part of their tests.\n\nTables\n\nfloat64_overrides: overrides of Base methods that use Float64 to compute single- or half-precision results.\n\n\n\n\n\n","title":"GPUToolbox.Overlays"},{"category":"constant","location":"overlays/#GPUToolbox.Overlays.float64_overrides","page":"Overlays","text":"GPUToolbox.Overlays.float64_overrides\n\nMethod table with overrides of Base methods that use Float64 to compute single- or half-precision results, for devices that don't support Float64 or where it is slow.\n\nThe overrides only replace Base's generic code, and call functions like sin, cos, sinpi, cospi and ^(::Float32, ::Float32), which Base also implements using Float64. Back-ends stacking this table should provide Float64-free implementations of those (e.g., using their hardware's math library) in a table higher up the stack.\n\nSee GPUToolbox.Overlays for how to use this table.\n\n\n\n\n\n","title":"GPUToolbox.Overlays.float64_overrides"},{"category":"function","location":"overlays/#GPUToolbox.Overlays.audit","page":"Overlays","text":"Overlays.audit(tables::Core.MethodTable...; world=Base.get_world_counter())\n\nCheck a stack of method tables, ordered from highest to lowest priority, and return a vector of (; table, method, issue, by) named tuples describing problems with the methods in these tables:\n\nissue = :dead: the method overrides a function from Base, but no method of that function matches its signature (e.g., because Base changed the function's signature). Methods of other functions, and methods for types defined in the table's module, aren't checked.\nissue = :shadowed or issue = :partly_shadowed: a method in table by, higher up the stack, covers all or part of the method's signature, so that (some) calls won't reach it. This is often intentional, e.g., when a back-end replaces a shared fallback.\n\n\n\n\n\n","title":"GPUToolbox.Overlays.audit"},{"category":"section","location":"#GPUToolbox.jl","page":"Home","text":"","title":"GPUToolbox.jl"},{"category":"section","location":"#API","page":"Home","text":"","title":"API"},{"category":"type","location":"#GPUToolbox.LazyInitialized","page":"Home","text":"LazyInitialized{T}()\n\nA thread-safe, lazily-initialized wrapper for a value of type T. Initialize and fetch the value by calling get!. The constructor is ensured to only be called once.\n\nThis type is intended for lazy initialization of e.g. global structures, without using __init__. It is similar to protecting accesses using a lock, but is much cheaper.\n\nAny value of type T, including nothing when allowed by T, may be stored.\n\n\n\n\n\n","title":"GPUToolbox.LazyInitialized"},{"category":"type","location":"#GPUToolbox.Literal","page":"Home","text":"Literal{T}\n\nConstruct an object that can be used to convert literals to other types. One can use the object in suffix form 1*i8 or 1i8 to perform the conversion.\n\nExported constants\n\ni8: Convert to Int8\ni16: Convert to Int16\ni32: Convert to Int32\nu8: Convert to UInt8\nu16: Convert to UInt16\nu32: Convert to UInt32\n\n\n\n\n\n","title":"GPUToolbox.Literal"},{"category":"type","location":"#GPUToolbox.SimpleVersion","page":"Home","text":"SimpleVersion(major, [minor])\nSimpleVersion(v::AbstractString)\n\nA GPU-compatible version number.\n\n\n\n\n\n","title":"GPUToolbox.SimpleVersion"},{"category":"macro","location":"#GPUToolbox.@checked-Tuple{Any}","page":"Home","text":"@checked function foo(...)\n    rv = ...\n    return rv\nend\n\nMacro for wrapping a function definition returning a status code. Two versions of the function will be generated: foo, with the function execution wrapped by an invocation of the check function (to be implemented by the caller of this macro), and unchecked_foo where no such invocation is present and the status code is returned to the caller.\n\n\n\n\n\n","title":"GPUToolbox.@checked"},{"category":"macro","location":"#GPUToolbox.@debug_ccall-Tuple{Any}","page":"Home","text":"@debug_ccall ...\n\nA version of ccall` that prints the ccall, its arguments, and its return value.\n\n\n\n\n\n","title":"GPUToolbox.@debug_ccall"},{"category":"macro","location":"#GPUToolbox.@gcsafe_ccall","page":"Home","text":"@gcsafe_ccall ...\n\nCall a foreign function just like @ccall, but marking it safe for the GC to run. This is useful for functions that may block, so that the GC isn't blocked from running, but may also be required to prevent deadlocks (see JuliaGPU/CUDA.jl#2261).\n\nNote that this is generally only safe with non-Julia C functions that do not call back into the Julia directly.\n\n\n\n\n\n","title":"GPUToolbox.@gcsafe_ccall"},{"category":"macro","location":"#GPUToolbox.@memoize-Tuple","page":"Home","text":"@memoize [key=expr::K | index=expr] begin\n    # expensive computation\nend::T\n\nLow-level, no-frills memoization macro that stores values in a process-local, typed cache. The types of the caches are derived from the syntactical type assertions. All return values, including nothing, are memoized correctly.\n\nWith no leading argument, a single value is memoized. Use key=expr::K for dictionary memoization keyed by values of type K. Use index=expr for array-backed memoization with small, dense, positive integer indices; sparse or large keys should use dictionary mode instead. Index mode grows the backing array on demand. On Julia 1.11+, index-mode slots use atomic per-element access. Atomic slot access is lock-free for pointer-representable values; other values may use Julia's internal per-element atomic fallback.\n\n\n\n\n\n","title":"GPUToolbox.@memoize"},{"category":"macro","location":"#GPUToolbox.@sv_str-Tuple{Any}","page":"Home","text":"sv\"str\"\n\nConstruct a SimpleVersion from str.\n\n\n\n\n\n","title":"GPUToolbox.@sv_str"}]
}
